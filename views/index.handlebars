<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-12">
        
        <h2>Ark INI list</h2>
       
          {{#each data}}
          
            <div data-id="{{this.id}}" class="rounded bg-success p-3 m-2">{{this.name}}</div>

          {{/each}}
        
      </div>
    </div>
  </div>
</div>

<script src="/js/index.js"></script>
